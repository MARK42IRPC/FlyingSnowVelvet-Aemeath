飞行雪绒 LTS1.0.5pre3 更新日志
==============================

版本发布日期：2026-04-15
本条目从 2026-04-15 起累计 pre3 阶段变更。

`pre3` 当前处于新一轮迭代起点，先完成版本号切换、文档入口同步与发布基线对齐；后续功能、修复与兼容性调整请继续直接追加到本文件与 `CHANGELOG.md`。

一、本次调整
------------
1. 项目版本号切换为 `LTS1.0.5pre3`。
   - `config/version_info.py` 已同步版本号与发布日期。
   - 打包脚本默认版本、资料舱门户版本展示会随之切换。

2. 仓库入口文档同步切换到 `pre3` 基线。
   - `README.md`
   - `CONTRIBUTING.md`
   - `RELEASING.md`
   - `doc/` 下中文说明文档

3. 资料舱门户将以 `pre3` 版本重新生成。
   - 执行命令：`python scripts/generate_doc_portal.py`

二、后续记录方式
----------------
1. 新功能、行为变化、修复项直接追加到本文件和 `CHANGELOG.md` 的 `LTS1.0.5pre3` 段落。
2. 若涉及运行目录、配置迁移或发布包结构变化，务必同步更新 `README.md` 与 `RELEASING.md`。
3. `pre2` 的已发布历史请继续以 `CHANGELOG.md` 中的 `LTS1.0.5pre2` 条目为准。

三、当前版本状态
----------------
- 当前版本：`LTS1.0.5pre3`
- 当前阶段：开发进行中
- 基线日期：`2026-04-15`
